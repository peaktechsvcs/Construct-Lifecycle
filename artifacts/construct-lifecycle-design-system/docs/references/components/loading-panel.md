# Loading Panel

**Source:** `artifacts/cabinet-projects/src/components/app-ui.tsx`, LoadingPanel export.

Card-shaped skeleton group with a configurable line count. Vary line widths so
the placeholder reads like real content rather than a progress meter.
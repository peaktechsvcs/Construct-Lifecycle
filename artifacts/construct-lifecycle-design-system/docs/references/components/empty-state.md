# Empty State

**Source:** the Construct Lifecycle web application's `src/components/app-ui.tsx`, EmptyState export.

Centered, dashed-border zero-data treatment with an icon tile, concise title,
supporting sentence, and optional action. Keep copy actionable and specific to
the missing content.
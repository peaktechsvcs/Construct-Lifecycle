{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "seed-projects": "tsx ./src/seed-projects.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:",
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
